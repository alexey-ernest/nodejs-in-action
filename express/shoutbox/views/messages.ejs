<% if (locals.messages) { %>
    <% locals.messages.forEach(function(message) { %>
        <p class='<%= message.type %>'><%= message.string %></p>
    <% }) %>
    <% locals.removeMessages() %>
<% } %>